"use strict";
// Make A Function Returns The Random Boolean;
const generateRandomBoolean = () => Math.random() > 0.5;
console.log(generateRandomBoolean());
// Explanation: The Function Returns the condition That Math.random() > 0.5 It Means if Number is > 0.5 So It Returns True Else False... And Math.random() Generates Different Number between 0.1 - 0.9 In Every Run So The Boolean Value Is Also Change According To The random Number..
