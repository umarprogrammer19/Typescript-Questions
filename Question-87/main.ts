// make the function that takes out the first 10 characters from any text
const takeOut = (str: string): string => str.substring(0, 10);
console.log(takeOut("I Have Completed My Assignments")); // Takeout 10 Letters
