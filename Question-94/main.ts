// Define array of words and find the length of all
const words: string[] = ["Hello", "World", "TypeScript", "JavaScript"];
const lengths: number[] = words.map(val => val.length);
console.log(lengths);


